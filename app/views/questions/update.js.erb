var questionId = <%= @question.id %>
var questionErrorsSelector = 'div#question-' + questionId + '-errors'
$(questionErrorsSelector).html('<%= render 'shared/errors', resource: @question %>');
<% if @question.errors.blank? %>
  $('#question-' + questionId).html('<%= j render @question %>');
  window.gistAsync();
<% end %>


