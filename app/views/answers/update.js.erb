var answerId = <%= @answer.id %>
var answerErrors = 'div#answer-' + answerId + '-errors'
$(answerErrors).html('<%= render 'shared/errors', resource: @answer %>');
<% if @answer.errors.blank? %>
  $('#answer-' + answerId).html('<%= j render @answer %>');
  window.gistAsync();
<% end %>
